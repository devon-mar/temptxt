---
version: "2"
linters:
  settings:
    errcheck:
      exclude-functions:
        - (*github.com/devon-mar/temptxt.TempTxt).OnFinalShutdown
        - (github.com/miekg/dns.ResponseWriter).WriteMsg
        - io.WriteString
        - net/http.Serve
formatters:
  enable:
    - gofumpt
